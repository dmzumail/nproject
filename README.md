# nproject — Infrastructure as Code в Yandex Cloud

> Развёртывание веб-сайта в облаке с помощью Terraform, GitHub Actions и Makefile.

Демо: [http://nproject.site](http://nproject.site)

---

## 🎯 Цель проекта

Показать основы DevOps-автоматизации:
- Создание инфраструктуры как код (IaC)
- Безопасное управление состоянием через Yandex Object Storage
- CI/CD для обновления инфраструктуры
- Упрощённое локальное управление через Makefile

---

## 🧰 Используемые технологии

- **Yandex Cloud**: ВМ, VPC, DNS, Object Storage
- **Terraform**: управление инфраструктурой
- **GitHub Actions**: автоматический `apply` при пуше в `main`
- **Makefile**: унификация локальных команд
- **Nginx**: веб-сервер с простым статическим контентом
- **Cloud-init**: первоначальная настройка ВМ

---

## 📁 Структура проекта

.
├── app/                    # Статический контент (HTML, CSS)
├── terraform/              # Конфигурация Terraform
│   ├── main.tf             # ВМ, DNS, security group
│   ├── network.tf          # Сеть и подсеть
│   ├── providers.tf        # Провайдер Yandex Cloud
│   ├── variables.tf        # Переменные
│   └── backend.tf          # Remote state в Object Storage
├── .github/workflows/
│   └── terraform.yml       # CI/CD для инфраструктуры
├── Makefile                # Управление локально
├── .gitignore              # Исключены tfstate и секреты
└── README.md

## ▶️ Локальное управление

Проект включает Makefile для упрощения рутинных операций:
make init         # Инициализация Terraform
make apply        # Применение инфраструктуры
make ssh          # Подключение к ВМ по SSH
make deploy-app   # Деплой статического контента из папки app/